<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds">

    <!-- ==================== 从 Spring 环境读取属性 ==================== -->
    <springProperty name="APP_NAME"   source="spring.application.name" defaultValue="ai-agent"/>
    <springProperty name="APP_ENV"    source="app.environment"         defaultValue="dev"/>
    <springProperty name="LOG_PATH"   source="logging.file.path"       defaultValue="logs"/>
    <springProperty name="LOG_LEVEL"  source="logging.level.root"      defaultValue="INFO"/>
    <springProperty name="APP_LEVEL"  source="logging.level.com.ai.agent" defaultValue="DEBUG"/>

    <!-- ==================== 日志格式 ==================== -->

    <property name="CONSOLE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %cyan([%X{traceId:-}]) %boldMagenta(%-40.40logger{39}) - %msg%n%ex"/>

    <property name="FILE_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{traceId:-}] %-40.40logger{39} - %msg%n%ex"/>

    <!-- ==================== Appender 定义 ==================== -->

    <!-- 1. 控制台 Appender（仅 dev / stag 激活） -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 2. 全量滚动文件 Appender（INFO 及以上，按天滚动，最多保留 30 天 / 2GB） -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- 3. ERROR 单独文件（按天滚动，保留 60 天） -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-error.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>60</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- ==================== 异步包装（提升 IO 吞吐） ==================== -->

    <appender name="ASYNC_FILE_ALL" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>1024</queueSize>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="FILE_ALL"/>
    </appender>

    <appender name="ASYNC_FILE_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <discardingThreshold>0</discardingThreshold>
        <queueSize>256</queueSize>
        <includeCallerData>true</includeCallerData>
        <appender-ref ref="FILE_ERROR"/>
    </appender>

    <!-- ==================== Logger 级别配置 ==================== -->

    <!-- 业务代码 -->
    <logger name="com.ai.agent" level="${APP_LEVEL}" additivity="false">
        <springProfile name="dev,stag">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE_ALL"/>
        <appender-ref ref="ASYNC_FILE_ERROR"/>
    </logger>

    <logger name="org.springframework"             level="INFO"/>
    <logger name="org.springframework.web.servlet" level="WARN"/>
    <logger name="org.hibernate"                   level="WARN"/>

    <logger name="okhttp3"            level="INFO"/>
    <logger name="okio"               level="INFO"/>

    <logger name="org.mybatis"        level="WARN"/>
    <logger name="com.ai.agent.mapper" level="INFO"/>

    <!-- ==================== Root Logger（兜底） ==================== -->

    <root level="${LOG_LEVEL}">
        <springProfile name="dev,stag">
            <appender-ref ref="CONSOLE"/>
        </springProfile>
        <appender-ref ref="ASYNC_FILE_ALL"/>
        <appender-ref ref="ASYNC_FILE_ERROR"/>
    </root>

</configuration>

